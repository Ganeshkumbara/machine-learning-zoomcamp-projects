services:
  - type: web
    name: salary-prediction-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn prediction:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
